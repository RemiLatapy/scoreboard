<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="remi.scoreboard">

    <application
        android:name=".ScoreBoardApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_score"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="remi.scoreboard.activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity-->
        <!--android:name=".activities.GameSimpleScoreActivity"-->
        <!--android:label="Game" />-->
        <!--<activity-->
        <!--android:name=".activities.GameChampionshipActivity"-->
        <!--android:label="Championship" />-->
    </application>

</manifest>